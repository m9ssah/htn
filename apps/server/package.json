{
  "name": "@jit/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "node": "tsc -b && node dist/cli.js",
    "live-jev": "tsc -b && node dist/live-jev.js",
    "record-jev-fixtures": "tsc -b && node dist/record-fixtures.js"
  },
  "dependencies": {
    "@json-render/core": "file:../../vendor/json-render-core-0.21.0-3ad3818-preview3.tgz",
    "@jit/renderer": "*",
    "@jit/schema": "*",
    "@jit/tokens": "*",
    "@langchain/langgraph": "^1.4.16"
  }
}
