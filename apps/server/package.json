{
  "name": "@jit/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "node": "tsc -b && node dist/cli.js",
    "live-jev": "tsc -b && node dist/live-jev.js"
  },
  "dependencies": {
    "@jit/schema": "*",
    "@jit/tokens": "*",
    "@jit/renderer": "*"
  }
}
